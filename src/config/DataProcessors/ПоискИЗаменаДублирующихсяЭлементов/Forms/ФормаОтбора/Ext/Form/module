    

Процедура ЗначенияОтбораПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	
	Если ДанныеСтроки.ИндексКартинки <> -1 Тогда
		ОформлениеСтроки.Ячейки.Представление.ИндексКартинки = ДанныеСтроки.ИндексКартинки;	
		ОформлениеСтроки.Ячейки.Представление.ОтображатьКартинку = Истина;
	КонецЕсли;
	
КонецПроцедуры
  
Процедура ВыбратьЗначение()
	
	СтруктураВыбора = Новый Структура;
	СтруктураВыбора.Вставить("Значение", ЭлементыФормы.ЗначенияОтбора.ТекущиеДанные.Значение);
	СтруктураВыбора.Вставить("Представление", ЭлементыФормы.ЗначенияОтбора.ТекущиеДанные.Представление);
	ОповеститьОВыборе(СтруктураВыбора);	
	
КонецПроцедуры

Процедура ЗначенияОтбораПередНачаломИзменения(Элемент, Отказ)
	
	Отказ = Истина;
	ВыбратьЗначение();
	
КонецПроцедуры

Процедура ПриОткрытии()
	
	Если НачальноеЗначение = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СтрокаТаблицы = ЗначенияОтбора.Найти(НачальноеЗначение , "Представление");
	Если СтрокаТаблицы = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЭлементыФормы.ЗначенияОтбора.ТекущаяСтрока = СтрокаТаблицы;
		
КонецПроцедуры

Процедура КнопкаОКНажатие(Элемент)
	
	ВыбратьЗначение();
	
КонецПроцедуры





