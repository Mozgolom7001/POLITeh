
Процедура КнопкаВыполнитьНажатие(Элемент)
	
	Если ВыполнятьНаСервере Тогда
	
		УправлениеЗапасамиПартионныйУчет.ВыполнитьВосстановлениеНаСервере (ДатаКонцаОбработки, 
										 Справочники.Организации.ПустаяСсылка(),
										 ОтражатьВУправленческомУчете,
										 Истина,
										 Истина,
										 Останавливаться,
										 МаксимальноеКоличествоДокументовВВыборке,
										 МаксимальноеКоличествоСтрокВТранзакции);
	Иначе
		
		ЗаполнениеДокументов.ВыполнитьВосстановление (ДатаКонцаОбработки,
								Справочники.Организации.ПустаяСсылка(),
								ОтражатьВУправленческомУчете,
								Истина,
								Истина,
								Останавливаться,
								МаксимальноеКоличествоДокументовВВыборке,
								МаксимальноеКоличествоСтрокВТранзакции);
		
	КонецЕсли;
	ОбновитьТекстОПоследовательностях();
	
КонецПроцедуры

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	ДатаКонцаОбработки = РабочаяДата;
	
	ОбновитьТекстОПоследовательностях();
	
	ОтражатьВУправленческомУчете = Истина;
	
КонецПроцедуры

Процедура ОбновитьТекстОПоследовательностях()
	
	СписокГраницПоТоварам.Очистить();;
	
	//Если ОтражатьВУправленческомУчете Тогда
		ГраницаТоварыУпр     = ПолучитьГраницуПоследовательности();
		
		СписокГраницПоТоварам.Добавить("Упр. учет: " + Формат(ГраницаТоварыУпр.Дата, "ДЛФ=DT") + ?(НЕ(ГраницаТоварыУпр.Ссылка=Неопределено), " (" +ГраницаТоварыУпр.Ссылка + ")",""));
	//КонецЕсли;
	
КонецПроцедуры

Процедура ДействияФормыОбновитьНадписи(Кнопка)
	
	ОбновитьТекстОПоследовательностях();
	
КонецПроцедуры

Процедура ОбновлениеОтображения()
	
	ОбновитьТекстОПоследовательностях();
	
КонецПроцедуры









