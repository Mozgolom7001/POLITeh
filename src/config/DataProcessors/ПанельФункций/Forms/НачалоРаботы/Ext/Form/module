////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

// Обработчик события "ПриОткрытии" формы.
// Считывает значения по умолчанию.
//
Процедура ПриОткрытии()
	
	Организация = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(глЗначениеПеременной("глТекущийПользователь"), "ОсновнаяОрганизация");
	
	
КонецПроцедуры // ПриОткрытии()

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМЕНТОВ ФОРМЫ

// Обработчик события "ПриИзменении" поля ввода "Организация".
// Устанавливает новое значение основной организации в настройках пользователя.
//
Процедура ОрганизацияПриИзменении(Элемент)
	
	УправлениеПользователями.УстановитьЗначениеПоУмолчанию(глЗначениеПеременной("глТекущийПользователь"), "ОсновнаяОрганизация", Организация);
	ОсновнаяОрганизация = Организация;
	
КонецПроцедуры // ОрганизацияПриИзменении()

// Обработчик нажатия на кнопку с крестиком.
//
Процедура ЗакрытьНажатие(Элемент)
	
	ЭтаФорма.ВладелецФормы.СкрытьСхему();
	
КонецПроцедуры // ЗакрытьНажатие()

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ НАЖАТИЯ НА ЗНАЧКИ (ОБЩИЕ)

// Процедура открытия нового документа.
//
Процедура ОткрытьДокументНовый(Элемент)
	
	ЭтаФорма.ВладелецФормы.ОткрытьДокументНовый(Элемент);
	
КонецПроцедуры // ОткрытьДокументНовый()

// Процедура открытия обработки.
//
Процедура ОткрытьОбработку(Элемент)
	
	ЭтаФорма.ВладелецФормы.ОткрытьОбработку(Элемент);
	
КонецПроцедуры // ОткрытьОбработку()

// Процедура открытия регистра сведений.
//
Процедура ОткрытьРегистрСведений(Элемент)
	
	ЭтаФорма.ВладелецФормы.ОткрытьРегистрСведений(Элемент);
	
КонецПроцедуры // ОткрытьРегистрСведений()

// Процедура открытия справочника.
//
Процедура ОткрытьСправочник(Элемент)
	
	ЭтаФорма.ВладелецФормы.ОткрытьСправочник(Элемент);
	
КонецПроцедуры // ОткрытьСправочник()

// Процедура открытия общей формы.
//
Процедура ОткрытьОбщуюФорму(Элемент)
	
	ПолучитьОбщуюФорму(Элемент.Имя).Открыть();
	
КонецПроцедуры // ОткрытьОбщуюФорму()

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ НАЖАТИЯ НА ЗНАЧКИ (СПЕЦИАЛЬНЫЕ)

// Открытие настроек текущего пользователя.
//
Процедура НастройкиПользователейНажатие(Элемент)
	ОткрытьФормуТекущегоПользователя();
КонецПроцедуры //НастройкиПользователейНажатие()

// Открытие регистра сведений ЗначенияПравПользователя.
//
Процедура ЗначенияПравПользователяНажатие(Элемент)
	РегистрыСведений.ЗначенияДополнительныхПравПользователя.ПолучитьФорму("ФормаРедактирования").Открыть();
КонецПроцедуры // ЗначенияПравПользователяНажатие()
