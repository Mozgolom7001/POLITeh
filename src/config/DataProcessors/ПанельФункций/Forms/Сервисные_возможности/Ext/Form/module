////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ


////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМЕНТОВ ФОРМЫ

// Обработчик нажатия на кнопку с крестиком.
//
Процедура ЗакрытьНажатие(Элемент)
	
	ЭтаФорма.ВладелецФормы.СкрытьСхему();
	
КонецПроцедуры // ЗакрытьНажатие()

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ НАЖАТИЯ НА ЗНАЧКИ (ОБЩИЕ)

// Процедура открытия обработки.
//
Процедура ОткрытьОбработку(Элемент)
	
	ЭтаФорма.ВладелецФормы.ОткрытьОбработку(Элемент);
	
КонецПроцедуры // ОткрытьОбработку()

// Процедура открытия общей формы.
//
Процедура ОткрытьОбщуюФорму(Элемент)
	
	ПолучитьОбщуюФорму(Элемент.Имя).Открыть();
	
КонецПроцедуры // ОткрытьОбщуюФорму()

////////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ НАЖАТИЯ НА ЗНАЧКИ (СПЕЦИАЛЬНЫЕ)

// Открытие обработки Обозреватель формы ОбновлениеКонфигурации.
//
Процедура ОбозревательНажатие(Элемент)
	Обработки.ОбновлениеКонфигурации.ПолучитьФорму("ОбновлениеКонфигурации").Открыть();
КонецПроцедуры // ОбозревательНажатие()

// Открытие обработки Обозреватель.
//
Процедура Обозреватель1Нажатие(Элемент)
	Обработки.Обозреватель.ПолучитьФорму("Форма").Открыть();
КонецПроцедуры // Обозреватель1Нажатие()

// Запуск процедуры отправки сообщения в отдел технической поддержки.
//
Процедура ОтправитьСообщениеВОтделТехническойПоддержкиНажатие(Элемент)
	УправлениеЭлектроннойПочтой.ОтправитьСообщениеВОтделТехническойПоддержки();
КонецПроцедуры // ОтправитьСообщениеВОтделТехническойПоддержкиНажатие()

// Открытие обработки Обозреватель.
//
Процедура МониторингЗаконодательстваНажатие(Элемент)
	ОбщегоНазначения.ПоказатьВебСтраницу("http://v8.1c.ru/lawmonitor/");
КонецПроцедуры

// Открытие обработки Обозреватель.
//
Процедура ИнформацияОФирмеНажатие(Элемент)
	ОбщегоНазначения.ПоказатьВебСтраницу("http://www.1c.ru");
КонецПроцедуры

// Открытие обработки Обозреватель.
//
Процедура ИнформацияОПрограммахНажатие(Элемент)
	ОбщегоНазначения.ПоказатьВебСтраницу("http://v8.1c.ru");
КонецПроцедуры

// Открытие обработки Обозреватель.
//
Процедура ПоискПартнеровНажатие(Элемент)
	ОбщегоНазначения.ПоказатьВебСтраницу("http://www.1c.ru/rus/partners/search.htm");
КонецПроцедуры

