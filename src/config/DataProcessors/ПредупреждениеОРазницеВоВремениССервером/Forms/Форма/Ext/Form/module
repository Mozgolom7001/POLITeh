//////////////////////////////////////////////////////////////////////////////// 
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМЕНТОВ ФОРМЫ
// 

Процедура ОсновныеДействияФормыПродолжить(Кнопка)

	УправлениеПользователями.УстановитьЗначениеПоУмолчанию(глЗначениеПеременной("глТекущийПользователь"), "ПредупреждатьОРазницеВремениССервером", НЕ ПропуститьПроверку);

	Закрыть(Истина);

КонецПроцедуры

Процедура ОсновныеДействияФормыВыйти(Кнопка)
	
	Закрыть();
	
КонецПроцедуры
