
// Обработчик события Нажатие элемента формы ДействияФормы.ПомощникКопирования.
//
Процедура ДействияФормыПомощникКопирования(Кнопка)
	Форма = РегистрыСведений.НоменклатураКонтрагентов.ПолучитьФорму("ФормаКопирования");
	Если Отбор.Контрагент.Использование Тогда
		Форма.КонтрагентИсточник = Отбор.Контрагент.Значение;
	КонецЕсли;
	Форма.Открыть();
КонецПроцедуры

// Процедура обработчик события при изменении номенклатуры
//
Процедура РегистрСведенийСписокНоменклатураПриИзменении(Элемент)

	мТекущиеДанные = ЭлементыФормы.РегистрСведенийСписок.ТекущиеДанные;
	Если мТекущиеДанные.ХарактеристикаНоменклатуры.Владелец <> Элемент.Значение Тогда
		мТекущиеДанные.ХарактеристикаНоменклатуры = Справочники.ХарактеристикиНоменклатуры.ПустаяСсылка();
	КонецЕсли;

КонецПроцедуры
